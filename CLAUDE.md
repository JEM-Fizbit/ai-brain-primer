# CLAUDE.md

This file exists so Claude-family tools that look for `CLAUDE.md` at the project root find a pointer. The canonical human-facing entry is [`README.md`](README.md); the primer itself is [`PRIMER_building_your_ai_brain.md`](PRIMER_building_your_ai_brain.md).

<!-- LAYERING: This file is injected alongside ~/.claude/CLAUDE.md (global).
     Do NOT repeat global rules here (git config, security, commit protocol, MCP globals).
     Only include project-specific context. -->

## Project Context

Public framework / methodology for building a personal AI Brain in plain Markdown. Content only — no build step, no code. Sibling to [`ai-brain-jem`](https://github.com/JEM-Fizbit/ai-brain-jem) (private Brain content) and [`brain-mcp-server`](https://github.com/JEM-Fizbit/brain-mcp-server) (generic MCP server that serves Brain content).

**Status:** Published reference doc.

## What NOT to do here

- Don't import JEM-specific content. This repo must stay generic/public.
- Don't duplicate brain-mcp-server tool docs or ai-brain-jem operational conventions.
